name: Run Unit Tests

on:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get install -y cmake libgtest-dev libgmock-dev

      - name: Build
        run: |
          cmake -S . -B build
          cmake --build build

      - name: Run Tests
        run: cd build && ctest